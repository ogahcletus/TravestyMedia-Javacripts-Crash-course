const name = "CletusOkibeOgah";

console.log(name);

// To determine the number of characters in a string, we use the length property

console.log(name.length);

//To change cases
console.log(name.toUpperCase());
console.log(name.toLowerCase());

//To pull a substring use

console.log(name.substring(0,6));

// change substring to another case

console.log(name.substring(0,6).toUpperCase());

//Can split a string into an array

console.log(name.split(''));













